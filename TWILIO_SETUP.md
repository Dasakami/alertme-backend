# AlertMe Backend - Полная документация

## 🚀 Обновления

### 1. Интеграция Twilio для SMS

#### Установка
```bash
pip install twilio
```

#### Настройка .env
```env
# Twilio (получите на https://www.twilio.com/console)
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1234567890  # Формат: +страна номер
```

#### Использование
```python
from notifications.sms_service import SMSService

sms_service = SMSService()

# Отправка SMS
success = sms_service.send_sms(
    to_phone="+996555123456",
    message="Ваш код подтверждения: 123456"
)

# Отправка MMS (с медиа)
success = sms_service.send_sms(
    to_phone="+996555123456",
    message="Ваше видео SOS",
    media_urls=["https://example.com/video.mp4"]
)
```

### 2. Система хранения медиа для SOS

#### Модели
- `SOSMediaLog` - логирование медиа файлов
- `MediaAccessToken` - токены доступа для ограничения просмотра

#### Использование
```python
from notifications.media_service import MediaService

# Сохранение файла
success, media_url, error = MediaService.save_media(
    file_obj=request.FILES['audio'],
    media_type='audio',
    sos_alert_id=123,
    user_id=456
)

# Генерация ссылки для SMS
media_link = MediaService.generate_media_link(sos_alert_id=123)

# Создание токена доступа (опционально)
token = MediaService.create_media_token(
    sos_alert_id=123,
    user_id=456,
    expires_hours=24
)
```

### 3. Улучшенная система подписок

#### API Endpoints

##### Получить текущую подписку
```
GET /api/subscriptions/current/
```

Ответ:
```json
{
    "id": 1,
    "plan": {
        "id": 1,
        "name": "Personal Premium",
        "plan_type": "personal_premium",
        "price_monthly": 100,
        "price_yearly": 1000
    },
    "status": "active",
    "is_premium": true,
    "days_remaining": 25,
    "end_date": "2025-01-31T12:00:00Z",
    "payment_period": "monthly",
    "auto_renew": false
}
```

##### Активировать код подписки
```
POST /api/subscriptions/activate/
Content-Type: application/json

{
    "code": "PROMO123ABC"
}
```

Ответ:
```json
{
    "success": true,
    "message": "Premium подписка активирована до 31.01.2025",
    "subscription": {
        "id": 1,
        "plan": "Personal Premium",
        "status": "active",
        "is_premium": true,
        "days_remaining": 30,
        "end_date": "2025-01-31T12:00:00Z"
    }
}
```

#### Функции кода ActivationCode

1. **Валидация кода**
   - Проверка активности
   - Проверка использования
   - Проверка истечения срока

2. **Пролонгация подписки**
   - Если у пользователя активная подписка того же плана, добавляет 30 дней
   - Если нет - создает новую подписку

3. **Логирование**
   - Сохраняет кто и когда активировал код
   - Сохраняет информацию о платеже из Telegram

### 4. SOS Уведомления через Twilio

#### Flow
1. Пользователь активирует SOS
2. Система отправляет SMS всем контактам через Twilio
3. Если есть медиа - включает ссылку на медиа в сообщение
4. Логирует все попытки отправки

#### Код задачи
```python
@shared_task
def send_sos_notifications(sos_alert_id, contact_ids):
    """Асинхронная отправка SOS уведомлений"""
    # Автоматически повторяет 3 раза при ошибке
    pass
```

## 📊 Миграции

После обновления кода выполните:
```bash
python manage.py makemigrations
python manage.py migrate
```

## 📝 Логирование

Все операции логируются в консоль и файлы:
- SMS отправка
- Активация кодов
- Ошибки подписок

Пример:
```
✅ SMS отправлено через Twilio на +996555123456 (SID: SM123456)
✅ Код PROMO123ABC активирован для +996555123456. Подписка до 31.01.2025
```

## 🧪 Тестирование

### Без Twilio (разработка)
Если `TWILIO_ACCOUNT_SID` не установлен, SMS отправляются в консоль:
```
============================================================
📱 SMS СООБЩЕНИЕ (CONSOLE MODE - Twilio не настроен)
============================================================
📞 Кому: +996555123456
📨 Текст: Ваш код подтверждения AlertMe: 123456...
============================================================
```

### С Twilio (продакшен)
SMS отправляются настоящие с ID: `SM123456...`

## 🔐 Безопасность

1. **Коды верификации**: истекают через 10 минут
2. **Коды активации**: действуют 30 дней
3. **Медиа токены**: истекают через 24 часа
4. **SMS**: максимум 10 MB для MMS

## 📱 API версия
- Django REST Framework 3.14.0
- JWT Authentication
- CORS включен

## ⚠️ Возможные ошибки

| Ошибка | Решение |
|--------|---------|
| `TWILIO_ACCOUNT_SID not set` | Добавьте в .env Twilio credentials |
| `Код уже использован` | Генерируйте новый код в боте |
| `Код истек` | Коды действуют 30 дней, создайте новый |
| `Пользователь не найден` | Проверьте номер телефона |
